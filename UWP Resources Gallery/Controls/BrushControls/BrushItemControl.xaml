<UserControl
    x:Class="UWPResourcesGallery.Controls.BrushControls.BrushItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPResourcesGallery.Controls.BrushControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Orientation="Horizontal" MinHeight="20" MinWidth="100">
        <TextBlock x:Name="BrushName" Text="{x:Bind Brush.Key}"/>

        <StackPanel RequestedTheme="Light" x:Name="LightContainer">
            <Border Background="{x:Bind Brush.LightHex}" Width="40" Height="40"/>
            <TextBlock x:Name="LightHexLabel" Text="{x:Bind Brush.LightHex}"/>
        </StackPanel>

        <StackPanel RequestedTheme="Dark" x:Name="DarkContainer">
            <Border Background="{x:Bind Brush.DarkHex}" Width="40" Height="40"/>
            <TextBlock x:Name="DarkHexLabel" Text="{x:Bind Brush.DarkHex}"/>
        </StackPanel>
            
    </StackPanel>
</UserControl>
